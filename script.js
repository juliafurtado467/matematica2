let numUm
let numDois

let resultadoSoma

numUm = Number(prompt("qual seria o primeiro número"));
numDois = Number(prompt("qual seria o segundo número"));

resultado = numUm + numDois 

alert(` o resultado da adição será: ${resultadoSoma}`)

let resultado

numUm = Number(prompt("Qual o primeiro número?"))
numDois = Number(prompt("Qual o segundo número?"))

resultado = numUm -  numDois

alert(`O resultado da subtração é: ${resultado}`)

let ResultadoDaDivisao

numUm = Number(prompt("escreva o primeiro numero"));
numDois = Number(prompt("escreva o segundo numero"));

ResultadoDaDivisao = numUm/numDois;

alert(`o resultado da divisão será ${ResultadoDaDivisao}`);

